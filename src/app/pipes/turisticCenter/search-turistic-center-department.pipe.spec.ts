import { SearchTuristicCenterDepartmentPipe } from './search-turistic-center-department.pipe';

describe('SearchTuristicCenterDepartmentPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchTuristicCenterDepartmentPipe();
    expect(pipe).toBeTruthy();
  });
});
