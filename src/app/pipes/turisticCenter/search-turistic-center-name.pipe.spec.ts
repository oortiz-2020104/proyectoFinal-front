import { SearchTuristicCenterNamePipe } from './search-turistic-center-name.pipe';

describe('SearchTuristicCenterNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchTuristicCenterNamePipe();
    expect(pipe).toBeTruthy();
  });
});
