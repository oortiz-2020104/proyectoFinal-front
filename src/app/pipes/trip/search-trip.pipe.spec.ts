import { SearchTripPipe } from './search-trip.pipe';

describe('SearchTripPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchTripPipe();
    expect(pipe).toBeTruthy();
  });
});
